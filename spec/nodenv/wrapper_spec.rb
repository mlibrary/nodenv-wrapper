require 'spec_helper'

describe Nodenv::Wrapper do
  it 'has a version number' do
    expect(Nodenv::Wrapper::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
